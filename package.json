{
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint --fix .",
    "release": "pnpm --dir packages/core release"
  },
  "devDependencies": {
    "@configurajs/eslint": "^0.0.8",
    "@configurajs/prettier": "^0.0.2",
    "eslint": "9.17.0",
    "prettier": "^3.2.5"
  }
}
